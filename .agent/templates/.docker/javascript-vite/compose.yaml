# =============================================================================
# Docker Compose - JavaScript/TypeScript com Vite (Otimizado com Multi-stage Cache)
# =============================================================================
# Uso: docker compose up
# App: http://localhost:5173
# =============================================================================

services:
  app:
    build:
      context: ../
      dockerfile: .docker/Dockerfile.node
      target: dev
    ports:
      - "5173:5173"
    volumes:
      - ../:/app
      - /app/node_modules
    working_dir: /app
    stdin_open: true
    tty: true
    env_file:
      - ../.env.development
    environment:
      - NODE_ENV=development
      - WATCHPACK_POLLING=true