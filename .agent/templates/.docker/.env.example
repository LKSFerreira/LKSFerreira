# =============================================================================
# TEMPLATE DE VARIÁVEIS DE AMBIENTE (DEVELOPMENT)
# =============================================================================
# Instrução: Copie para a raiz do projeto como '.env.development'
# =============================================================================

# --- Banco de Dados (PostgreSQL) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=nome_do_projeto_db

# --- Conexão (Internal Docker Network) ---
# IMPORTANTE: O host deve ser 'database' (nome do serviço no compose.yaml)
DATABASE_URL=postgresql://postgres:postgres@database:5432/nome_do_projeto_db

# --- Configurações de Ambiente ---
# Escolha a variável correspondente à sua stack e remova as outras:
NODE_ENV=development
PYTHON_ENV=development
APP_ENV=local

# --- Portas Expostas ---
# (Certifique-se de que coincida com o compose.yaml escolhido)
PORT=8000