# =============================================================================
# Template de Variáveis de Ambiente
# =============================================================================
# Copie este arquivo para .env.development na raiz do projeto
# =============================================================================

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=meu_banco

# Conexão com o banco (usado pelo app)
# Nota: 'database' é o nome do serviço no Docker Compose
DATABASE_URL=postgresql://postgres:postgres@database:5432/meu_banco

# Ambiente
NODE_ENV=development
# ou
# PYTHON_ENV=development
# RAILS_ENV=development
# APP_ENV=local
